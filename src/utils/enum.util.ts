export enum TaskPriority {
  HIGH = "High",
  MEDIUM = "Medium",
  LOW = "Low",
}

export enum TaskStatus {
  PENDING = "Pending",
  COMPLETED = "Completed",
}

export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}
